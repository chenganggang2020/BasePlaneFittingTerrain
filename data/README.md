# Data Folder

Use this folder for generated or imported datasets.

Recommended usage:

- put DEM folders here
- put point-cloud folders here
- keep generated names such as `dem_files_output_*` and `point_clouds_output_*`
- avoid mixing experiment plots or paper notes into this directory

This repository ignores generated data folders under `data/` by default.
